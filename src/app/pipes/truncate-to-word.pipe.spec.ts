import { TruncateToWordPipe } from './truncate-to-word.pipe';

describe('TruncateToWordPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateToWordPipe();
    expect(pipe).toBeTruthy();
  });
});
