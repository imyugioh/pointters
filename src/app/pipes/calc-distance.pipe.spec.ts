import { CalcDistancePipe } from './calc-distance.pipe';

describe('CalcDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
